# Carrick 工具箱

现代化的多合一网页实用工具集合，致力于为用户提供简洁、高效、美观的日常工具体验。

## 🚀 快速开始

### 方法1: 使用Node.js服务器（推荐）

```bash
# 安装依赖（如果需要）
npm install

# 启动开发服务器
npm start
```

然后在浏览器中访问 `http://localhost:3000`

### 方法2: 使用Live Server

如果你使用VS Code，可以安装Live Server扩展，右键点击`index.html`选择"Open with Live Server"。

### 方法3: 使用Python服务器

```bash
# Python 3
python -m http.server 3000

# Python 2
python -m SimpleHTTPServer 3000
```

## 📁 项目结构

```
carrick-toolbox/
├── index.html           # 入口文件
├── styles.css           # 全局样式
├── main.js             # 核心调度逻辑
├── server.js           # 开发服务器
├── package.json        # 项目配置
├── utils/              # 公共工具
│   ├── domHelper.js    # DOM操作辅助
│   └── formatting.js   # 格式化工具
├── lib/                # 第三方库
└── tools/              # 工具模块
    ├── clock/          # 模拟时钟
    ├── time-converter/ # 时间转换
    ├── color-converter/ # 颜色转换
    ├── currency-converter/ # 货币转换
    ├── markdown/       # Markdown编辑器
    ├── regex-tester/   # 正则测试
    └── json-formatter/ # JSON格式化
```

## 🛠️ 功能特性

- ⏰ **时钟工具**：模拟时钟显示
- ⏰ **时间工具**：时区转换、时间戳计算
- 🎨 **颜色工具**：HEX/RGB/HSL/HSV格式转换
- 💰 **货币工具**：实时汇率换算
- 📝 **写作工具**：Markdown编辑器
- 🔧 **开发者工具**：正则测试、JSON格式化
- ⚙️ **系统功能**：明暗主题切换、响应式布局

## 🎨 设计特色

- 🌿 清新的青绿色主题配色
- 🌓 明暗模式切换
- 📱 响应式设计，适配各种设备
- ⚡ 流畅的动画和交互
- 🎯 简洁直观的用户界面

## 📖 开发说明

### 技术栈
- **HTML5** + **CSS3** + **Vanilla JavaScript (ES6+)**
- **模块化架构**：动态import，按需加载
- **CSS变量**：主题系统
- **SVG**：矢量图形

### 添加新工具

1. 在`tools/`目录下创建新工具文件夹
2. 创建`index.js`和`style.css`文件
3. 在`main.js`中注册工具配置
4. 在`index.html`中添加导航项

### 工具模板

```javascript
// tools/your-tool/index.js
import { domHelper } from '../../utils/domHelper.js';

export default class YourTool {
  constructor(container) {
    this.container = container;
  }

  async init() {
    this.render();
    this.bindEvents();
  }

  render() {
    this.container.innerHTML = `
      <div class="your-tool">
        <!-- 工具内容 -->
      </div>
    `;
  }

  bindEvents() {
    // 绑定事件
  }

  destroy() {
    // 清理资源
  }
}
```

## 📄 PTT框架文档

项目采用PTT（Product, Tech, Task）框架进行管理：

- 📋 **Product Spec**：产品需求和功能定义
- 🔧 **Tech Spec**：技术架构和实现规范
- 📝 **Task Spec**：开发任务和进度跟踪

文档位于`.doc/`目录下。

## 🤝 贡献

欢迎提交Issue和Pull Request！

## 📜 许可证

MIT License
